# Database Configuration - For Render PostgreSQL
spring.datasource.url=jdbc:postgresql://dpg-d6hgggpdrdic73cm9bj0-a.singapore-postgres.render.com:5432/food_delivery_pypy
spring.datasource.username=food_user
spring.datasource.password=hSxTZG05RjdNn3ewXEiQp5iPIkNQ6ieD
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration - For PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Server Configuration
server.port=${PORT:8080}

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.tracking-modes=cookie

# Security Configuration
spring.security.remember-me.key=uniqueAndSecret
spring.security.remember-me.token-validity-seconds=86400